- get client and server subdirectories from environment variables
  ELIOM_CLIENT_DIR and ELIOM_SERVER_DIR and use them in 
  dependencies and targets
- serge/staff in Ocsigen configuration file should be templated with user name and group

